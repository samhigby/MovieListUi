import { AverageRatingPipe } from './average-rating.pipe';

describe('AverageRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new AverageRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
